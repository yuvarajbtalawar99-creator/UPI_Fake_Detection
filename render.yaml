services:
  - type: web
    name: kavach-api
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free # Adjust to 'starter' if the free tier runs out of memory (OOM) due to TensorFlow/EasyOCR
    healthCheckPath: /api/health
    branch: main
